<%= render partial: 'application/errors', locals: {object: @review} %>

<div class="wrapper">
  <h2>Leave a review for</h2>
  <h2><%= @review.course.name %></h2>
  <%= render partial: 'form', locals: {review: @review} %>
</div>