<%= render partial: 'application/errors', locals: {object: @klass} %>

<h1>Edit a Class</h1>
<%= form_for @klass, url: class_path(@klass) do |f| %>

<%= render partial: 'form', locals: {f: f} %>
<%= f.submit %>

<% end %>